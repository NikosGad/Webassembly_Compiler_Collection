version: "3"
services:
    adminer:
        image: adminer
        container_name: adminer
        ports:
            - "8000:8080"
    ucrm_db:
        build:
            context: ./backend/database
            dockerfile: Dockerfile
        image: ucrm_db:${IMAGE_VERSION}
        container_name: ucrm_db
        user: postgres
        environment:
            - POSTGRES_PASSWORD=example
            - POSTGRES_USER=database
        volumes:
            - "${HOST_RESULTS_DIR_PREFIX}/wasmcc_volumes/database_volume_prod:/var/lib/postgresql/data"
    nginx:
        build:
            context: ./frontend/
            dockerfile: Dockerfile-nginx
        image: nginx:${IMAGE_VERSION}
        container_name: nginx
        user: nginx
        ports:
            - "${FRONTEND_HOST_PORT}:${FRONTEND_CONTAINER_PORT}"
            - "${BACKEND_HOST_PORT}:${BACKEND_CONTAINER_PORT}"
        depends_on:
            - rest_server
    rest_server:
        build: ./backend/rest_server
        image: ${APP}:${IMAGE_VERSION}
        user: ${APP_USER}
        container_name: wasmcc
        environment:
            - UPLOAD_PATH_EMSCRIPTEN=${ROOT_RESULTS_DIR}/${EMSCRIPTEN}/
            - UPLOAD_PATH_GOLANG=${ROOT_RESULTS_DIR}/${GOLANG}/src/
            - GOOS=js
            - GOARCH=wasm
            - GOCACHE=${ROOT_RESULTS_DIR}/${GOLANG}/dev_tools/.cache/go-build
            - GOENV=${ROOT_RESULTS_DIR}/${GOLANG}/dev_tools/.config/go/env
            - GO_INSTALLATION_PATH=/usr/local/go/
            - POSTGRES_PASSWORD=example
            - POSTGRES_USER=database
            - JWT_SECRET_KEY=jwt_secret_key
        expose:
            - ${BACKEND_CONTAINER_PORT}
        volumes:
            - "${HOST_RESULTS_DIR_PREFIX}/wasmcc_results${ROOT_RESULTS_DIR}:${ROOT_RESULTS_DIR}"
        command: bash -c "until python database_accepts_connections.py; do sleep 5; done; uwsgi --ini /app/uwsgi.ini"
        depends_on:
            - ucrm_db
