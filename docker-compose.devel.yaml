version: "3"
services:
    adminer:
        image: adminer
        container_name: adminer-devel
        ports:
            - "8000:8080"
    database:
        build:
            context: ./backend/database
            dockerfile: Dockerfile
        image: database:${IMAGE_VERSION}
        container_name: database-devel
        # user: ${APP_USER}
        environment:
            - POSTGRES_PASSWORD=example
            - POSTGRES_USER=database
    nginx:
        build:
            context: ./frontend/
            dockerfile: Dockerfile
        image: angular_cli:${IMAGE_VERSION}
        container_name: angular_cli_devel
        ports:
            - "${FRONTEND_HOST_PORT}:4200"
        volumes:
            - "/home/nikos/HMMY\ EMP/Diploma\ Thesis/Webassembly_Compiler_Collection/frontend/app-frontend:/home/node/app-frontend"
        command: bash -c "ng serve --host 0.0.0.0"
    rest_server:
        build: ./backend/rest_server
        image: ${APP}-devel:${IMAGE_VERSION}
        user: ${APP_USER}
        container_name: wasmcc-devel
        environment:
            - UPLOAD_PATH_EMSCRIPTEN=${ROOT_RESULTS_DIR}/${EMSCRIPTEN}/
            - UPLOAD_PATH_GOLANG=${ROOT_RESULTS_DIR}/${GOLANG}/src/
            - GOOS=js
            - GOARCH=wasm
            - GOCACHE=${ROOT_RESULTS_DIR}/${GOLANG}/dev_tools/.cache/go-build
            - GOENV=${ROOT_RESULTS_DIR}/${GOLANG}/dev_tools/.config/go/env
            - GO_INSTALLATION_PATH=/usr/local/go/
        ports:
            - "${BACKEND_HOST_PORT}:${BACKEND_CONTAINER_PORT}"
        volumes:
            - "./backend/rest_server:/app"
            - "${HOST_RESULTS_DIR_PREFIX}${ROOT_RESULTS_DIR}:${ROOT_RESULTS_DIR}"
        command: bash -c "FLASK_APP=/app/wsgi.py FLASK_ENV=development python3 /app/wsgi.py"
        depends_on:
            - database
